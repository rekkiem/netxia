# Bloquear acceso web a datos sensibles.
# El índice del blog se sirve solo vía /php/blog.php
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
